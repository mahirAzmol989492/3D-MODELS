# 3D-MODELS
The 3D MODEL (partially AI-built) repository consists of multiple 3D models, as the name suggest. The main purpose of these models is fully educational  and helps the student visualize the contents at some extent. Each file in this repository contains  a different 3D model.

##########

Used language ->>>>> Python with openGL library . Without using every openGL library function , codes here uses some manually built skeleton.


